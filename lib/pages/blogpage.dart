import 'package:flutter/material.dart';

class BlogPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Blog'),
      ),
      //IOS vs Android
      //Web app vs mobile app vs website(html)
      //Cloud vs traditional server based
      //lean development priciples
      //Idea to production steps

      body: Center(
        child: Text('Blog Page'),
      ),
    );
  }
}
